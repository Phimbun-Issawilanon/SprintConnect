import { DeliveryTypeTable } from './delivery-type-table.model';

describe('DeliveryTypeTable', () => {
  it('should create an instance', () => {
    expect(new DeliveryTypeTable()).toBeTruthy();
  });
});
