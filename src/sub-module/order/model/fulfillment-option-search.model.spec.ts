import { FulfillmentOptionSearch } from './fulfillment-option-search.model';

describe('FulfillmentOptionSearch', () => {
  it('should create an instance', () => {
    expect(new FulfillmentOptionSearch()).toBeTruthy();
  });
});
