import { FulfillmentOptionTable } from './fulfillment-option-table.model';

describe('FulfillmentOptionTable', () => {
  it('should create an instance', () => {
    expect(new FulfillmentOptionTable()).toBeTruthy();
  });
});
