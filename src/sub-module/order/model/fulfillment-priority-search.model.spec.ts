import { FulfillmentPrioritySearch } from './fulfillment-priority-search.model';

describe('FulfillmentPrioritySearch', () => {
  it('should create an instance', () => {
    expect(new FulfillmentPrioritySearch()).toBeTruthy();
  });
});
