import { FulfillmentPriorityTable } from './fulfillment-priority-table.model';

describe('FulfillmentPriorityTable', () => {
  it('should create an instance', () => {
    expect(new FulfillmentPriorityTable()).toBeTruthy();
  });
});
