import { FulfillmentTypeSearch } from './fulfillment-type-search.model';

describe('FulfillmentTypeSearch', () => {
  it('should create an instance', () => {
    expect(new FulfillmentTypeSearch()).toBeTruthy();
  });
});
