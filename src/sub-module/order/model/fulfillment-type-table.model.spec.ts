import { FulfillmentTypeTable } from './fulfillment-type-table.model';

describe('FulfillmentTypeTable', () => {
  it('should create an instance', () => {
    expect(new FulfillmentTypeTable()).toBeTruthy();
  });
});
