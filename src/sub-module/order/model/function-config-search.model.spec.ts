import { FunctionConfigSearch } from './function-config-search.model';

describe('FunctionConfigSearch', () => {
  it('should create an instance', () => {
    expect(new FunctionConfigSearch()).toBeTruthy();
  });
});
