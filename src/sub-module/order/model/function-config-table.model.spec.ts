import { FunctionConfigTable } from './function-config-table.model';

describe('FunctionConfigTable', () => {
  it('should create an instance', () => {
    expect(new FunctionConfigTable()).toBeTruthy();
  });
});
