import { RunningNumberSearch } from './running-number-search.model';

describe('RunningNumberSearch', () => {
  it('should create an instance', () => {
    expect(new RunningNumberSearch()).toBeTruthy();
  });
});
