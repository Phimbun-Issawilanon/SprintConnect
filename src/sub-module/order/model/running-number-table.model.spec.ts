import { RunningNumberTable } from './running-number-table.model';

describe('RunningNumberTable', () => {
  it('should create an instance', () => {
    expect(new RunningNumberTable()).toBeTruthy();
  });
});
